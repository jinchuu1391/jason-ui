name: Create jira issue from github issue

on:
  issues:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            const githubId = context.payload.issue.assignee.login
            const accountIds = {
              jinchuu1391: '6271faf33d0b58006946fe0c'
            }
            const myId = accountIds.githubId
            console.log(myId)
